interface:
  display_name: "CodeFlowLens"
  short_description: "Understand code flow with HTML sequence diagrams"
  default_prompt: "Use $codeflowlens to diagram this code execution flow for review."

policy:
  allow_implicit_invocation: true
